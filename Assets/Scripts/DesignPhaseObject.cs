using UnityEngine;

// Put this on each of the 5 clickable desk objects.
// Drag the matching PhaseData asset into the 'data' slot in the Inspector.
public class DesignPhaseObject : MonoBehaviour
{
    public PhaseData data;
}
